import React from "react";
import styled from "styled-components";

const StyledFooter = styled.footer`
  position: fixed;
  bottom: 0;
  width: 100%;
  background: black;
  color: white;
`;

const Footer = () => {
  return <StyledFooter>(c) Rasim Khusaenov</StyledFooter>;
};

export default Footer;
